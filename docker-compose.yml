version: '3'
services:
  redis:
    image: redis:latest
    container_name: redis-container
    environment:
      - REDIS_PASSWORD=S9v!mQ2#xL7@pR4$kN8  # שנה את הסיסמה כאן
    ports:
      - "6379:6379"
    command: ["redis-server", "--requirepass", "S9v!mQ2#xL7@pR4$kN8"]  # מפעיל את Redis עם הסיסמה

